/**
 * Colors used by generated assets (favicon, Open Graph images).
 * Keep these in sync with the CSS tokens in `src/app/globals.css`.
 */
export const theme = {
  background: "#080B09",
  backgroundSecondary: "#0E1510",
  surface: "#141C16",
  foreground: "#F3F5F1",
  secondary: "#C2C8C2",
  muted: "#8D948E",
  border: "rgba(210, 220, 212, 0.12)",
  accent: "#5AD68C",
  accentMuted: "#2FA866",
} as const;
