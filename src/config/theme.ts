/**
 * Colors used by generated assets (favicon, Open Graph images).
 * Keep these in sync with the CSS tokens in `src/app/globals.css`.
 * The live site reads from CSS; this file exists because ImageResponse
 * cannot see stylesheet variables.
 */
export const theme = {
  background: "#0b0b0a",
  backgroundSecondary: "#121211",
  surface: "#161613",
  foreground: "#f0eee6",
  secondary: "#b4b1a5",
  muted: "#7c7a70",
  border: "#2a2a25",
  accent: "#8e9763",
  accentMuted: "#5d6448",
} as const;
